import React from "react";

export const authContext = React.createContext();
const INIT_STATE = {};
const AuthContextProvider = ({ children }) => {
  return <AuthContextProvider.Provider></AuthContextProvider.Provider>;
};
